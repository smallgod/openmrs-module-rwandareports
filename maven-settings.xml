<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
    <servers>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-modules</id>
        </server>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-releases</id>
        </server>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-snapshots</id>
        </server>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-contrib</id>
        </server>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-modules-pih</id>
        </server>
        <server>
            <username>${env.MAVEN_REPO_MODULES_PIH_USERNAME}</username>
            <password>${env.MAVEN_REPO_MODULES_PIH_PASSWORD}</password>
            <id>openmrs-repo-modules-pih-snapshots</id>
        </server>
    </servers>
</settings>
